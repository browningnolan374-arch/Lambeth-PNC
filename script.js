// script.js\n\n// Sample records for demonstration\nconst records = [\n    { id: 1, name: "John Doe", info: "Record details for John Doe." },\n    { id: 2, name: "Jane Smith", info: "Record details for Jane Smith." },\n    { id: 3, name: "Alice Johnson", info: "Record details for Alice Johnson." },\n];\n\n// Function to display records\nfunction displayRecords(recordsToDisplay) {\n    const recordContainer = document.getElementById('recordContainer');\n    recordContainer.innerHTML = ''; // Clear previous records\n    \n    recordsToDisplay.forEach(record => {\n        const recordElement = document.createElement('div');\n        recordElement.classList.add('record');\n        recordElement.innerHTML = `<h3>${record.name}</h3><p>${record.info}</p>`;\n        recordContainer.appendChild(recordElement);\n    });\n}\n\n// Function to search and display results\nfunction searchRecords() {\n    const query = document.getElementById('searchInput').value.toLowerCase();\n    const filteredRecords = records.filter(record => record.name.toLowerCase().includes(query));\n    displayRecords(filteredRecords);\n}\n\n// Event listener for search input\ndocument.getElementById('searchInput').addEventListener('input', searchRecords);\n\n// Initial display of all records\ndisplayRecords(records);\n